## Template

```lua
vim.bo.tabstop = <num>     -- size of a hard tabstop (ts).
vim.bo.softtabstop = <num> -- number of spaces a <Tab> counts for. When 0, feature is off (sts).
vim.bo.shiftwidth = <num>  -- size of an indentation (sw).
vim.opt.expandtab = <bool> -- replace tab with spaces
```
